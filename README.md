# Calculator

تطبيق صغير بسيط لحساب العمليات الأربع (+ - \* /) مع واجهة تفاعلية مبنية باستخدام Flutter.

## 📱 الفكرة

واجهة واحدة تعرض شاشة النتائج وشبكة أزرار منسدلة لكل الأرقام والعمليات. كل ضغطة على زر تُحدّث الناتج بشكل فوري حتى يتم الضغط على `=` لحساب النتيجة النهائية.

## ⚡ المميزات

- تخزين مؤقت للعدد الحالي والمشغل لاستخدام متتالٍ للعمليات.
- أزرار مخصصة قابلة لإعادة الاستخدام (`CalculateButton`) مع تصميم حديث (ظل، زوايا مدورة).
- دعم الأعداد العشرية والنسبة إلى العمليات الرياضية الأربع الأساسية.
- واجهة بسيطة تعتمد على `StatefulWidget` لإدارة النص المعروض ديناميكيًا.

## 🏗️ البنية

```
lib/
├── main.dart            # نقطة الدخول التي تهيئ التطبيق وتعرض CalculateView
├── calculate_view.dart  # الصفحة الرئيسية للحسابات: شاشة النتائج، صفوف الأزرار، ومنطق الحساب
└── calculate_button.dart# زر عام يُستخدم لكل رقم أو مشغل لتقليل التكرار
```

المنطق كله موجود داخل `CalculateView`: يتم حفظ العدد الأول والمشغل ثم حساب النتيجة عندما يضغط المستخدم زر `=` أو مشغل آخر.

## 🛠️ التقنيات

- Flutter 3.x مع Material 3
- Stateful widgets لإدارة الحالة
- Components مخصصة لإعادة الاستخدام (الأزرار)

## 🧰 تشغيل المشروع

```bash
flutter clean
flutter pub get
flutter run
```

## 📝 ملاحظات تطوير

- واجهة خفيفة وسهلة التوسع (يمكن إضافة عمليات أو اختصارات مستقبلًا).
- الكود مستعد لإعادة هيكلة أكثر تعقيدًا (مثل فصل الحسابات إلى خدمة) دون تغيير الواجهة الحالية.
- التركيز على وضوح الحالة وقراءة الأزرار بدلًا من إدارة أرقام متفرعة في عدة أماكن.
